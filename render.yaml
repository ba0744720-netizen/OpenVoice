services:
  - type: web
    name: openvoice-web
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api:app --host 0.0.0.0 --port 10000"
    plan: starter  # change to paid plan name if you have one